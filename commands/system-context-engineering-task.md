---
description: Route a task to the smallest sufficient set of service contexts
---

Invoke the Qwen Code Skill tool for `system-context-engineering`. Execute only
the `task-context` operation for the task below. Read the compact system routing
artifacts first and load only routed service context sections. Do not regenerate
documentation.

Return relevant services, cross-service edges, canonical pointers, change-impact
surfaces, recorded verification routes, freshness warnings, and unresolved
assumptions.

Task:

```text
{{args}}
```
