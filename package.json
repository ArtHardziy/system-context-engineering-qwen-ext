{
  "name": "@protone/system-context-engineering",
  "version": "0.1.0",
  "description": "Qwen Code extension for aggregating microservice repository context into a system map and routing index.",
  "license": "UNLICENSED",
  "files": [
    "qwen-extension.json",
    "commands",
    "skills"
  ],
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "validate": "node scripts/validate-package.mjs",
    "prepack": "npm test && npm run validate"
  },
  "keywords": [
    "qwen-code",
    "qwen-extension",
    "agent-skill",
    "microservices",
    "system-context"
  ]
}
